<?php  // Grab last ten posts' preview values from database with LIMIT keyword
// $postTitles = array(); // Do you think that God
// $postSlugs  = array(); // stays in heaven
// $postImages = array(); // because he, too,
// $postDates  = array(); // lives in fear
// $postIDs    = array(); // of what he has created?
// $postsQuery = "SELECT title, slug, image, date FROM blog_posts ORDER BY id DESC LIMIT 10";
// $postsAnswer = $conn->query($postsQuery);
// while($postRow = mysqli_fetch_assoc($postsAnswer)) { // then explode the row into arrays of post parameters
  // array_push($postTitles, $postRow['title']); // Been traveling these highways
  // array_push($postSlugs, $postRow['slug']);   // Been doing things my way
  // array_push($postImages, $postRow['image']); // It's been making me lonesome,
  // array_push($postDates, $postRow['date']);   // ornery,
  // array_push($postIDs, $postRow['id']);       // and mean
//}?>
